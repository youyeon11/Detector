[pytest]
pythonpath = .
addopts = -ra
norecursedirs = .pytest_cache pytest-cache-files-* venv .venv
markers =
    unit: fast isolated tests with fakes or local fixtures only
    integration: API and service integration tests without external ES dependency
    es: tests that require a live Elasticsearch instance
